# R session / history / workspace images
.Rproj.user
.Rhistory
.RData
.Ruserdata
.RDataTmp*

# Claude Code local config and worktrees
.claude/

# rmarkdown/kableExtra self-contained render libraries (regenerated on render)
/lib*/

# Local backups / scratch
.backups/

# OS files
.DS_Store
Thumbs.db
.positai

# Personal manuscript / scratch data (not part of the code release)
depressionsnetzwerk_manuskript.*
dat_sim.rds

# Transient analysis outputs written at the repo root (regenerated by the pipeline)
/interpretation_*.csv
/interpretation_*.rds
/model_comparison_all_outcomes.csv
/random_effects_recommendations.csv
/run_parts4_5_output.txt

# Simulation/estimation data: only the final published run is tracked (via LFS).
# Ignore every timestamped run, intermediate Timeseries data, and session info.
output/*.rds
!output/MSAR_models_final.rds
!output/CONFIG_final.rds
output/Timeseries_data_*.rds
!output/Timeseries_data_final.rds
output/sessionInfo_*.txt

# NOTE: the final figures and tables are intentionally tracked -- do NOT add
# global *.pdf / *.tex / *.html ignores here, or output/plots/ and
# output/results/ (and docs/*.pdf) would be excluded.
